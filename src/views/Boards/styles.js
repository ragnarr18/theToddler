import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  toolbarAction: {
    backgroundColor: '#faa',
    padding: 10,
    flex: 1,
    alignItems: 'center',
  },

  toolbar: {
    flexDirection: 'row',
    height: 80,
    backgroundColor: '#ffa',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
