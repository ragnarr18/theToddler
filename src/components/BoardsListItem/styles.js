import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },

  toolbar: {
    flexDirection: 'row',
    height: 80,
    backgroundColor: '#ffa',
  },
});

export default styles;
