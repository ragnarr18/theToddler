import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    flexGrow: 1,
    height: 100,
  },

  boarderItem: {
    margin: 10,
  },

  boardItemLongPress: {
    backgroundColor: 'red',
  },
});

export default styles;
