import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listOverhead: {
    flex: 1,
    borderWidth: 3,
  },

  listText: {
    alignSelf: 'center',
    fontWeight: 'bold',
    fontSize: 17,
    padding: 15,
  },
});
