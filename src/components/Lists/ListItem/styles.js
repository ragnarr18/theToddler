import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  listOverhead: {
    flex: 1,
    borderWidth: 3,
  },

  listText: {
    fontWeight: 'bold',
    fontSize: 16,
    padding: 10,
  },
});
