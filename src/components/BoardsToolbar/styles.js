import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 10,
  },

  toolbar: {
    flexDirection: 'row',
    height: 80,
    backgroundColor: '#ffa',
  },
});
