import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
  },

  boardItem: {
    margin: 10,
  },

  boardItemLongPress: {
    backgroundColor: 'red',
  },
});

export default styles;
